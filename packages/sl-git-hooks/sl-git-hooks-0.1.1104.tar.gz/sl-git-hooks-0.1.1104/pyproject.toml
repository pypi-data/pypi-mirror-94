[tool.poetry]
name = "sl-git-hooks"
version = "0.1.1104"
description = ""
authors = ["Alexander Memer <mkoaleksedos@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.6"
pygit2 = "^1.5.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
sl-git-hooks = 'sl_git_hooks:main'