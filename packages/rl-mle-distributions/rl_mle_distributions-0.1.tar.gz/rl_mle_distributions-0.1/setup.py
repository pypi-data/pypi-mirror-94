from setuptools import setup

setup(name='rl_mle_distributions',
      version='0.1',
      description='Gaussian and binomial distributions',
      packages=['rl_mle_distributions'],
      zip_safe=False)
