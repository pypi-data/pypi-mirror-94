#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

install_requires = \
['Jinja2', 'typer']

entry_points = \
{'console_scripts': ['configtor = configtor:main']}

setup(name='configtor',
      version='0.0.2',
      description='configuration tool base on jinja2',
      author='ChienHsiao',
      author_email='chien.hsiao@revteltech.com',
      url=None,
      py_modules=['configtor'],
      install_requires=install_requires,
      entry_points=entry_points,
     )
