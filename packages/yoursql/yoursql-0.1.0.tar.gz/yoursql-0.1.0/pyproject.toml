[tool.poetry]
name = "yoursql"
version = "0.1.0"
description = "An ORM for SQL backends"
authors = ["trumanpurnell <truman.purnell@gmail.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
