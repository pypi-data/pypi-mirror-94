[flake8]
max-line-length = 120
exclude = build/

[build_sphinx]
source-dir = doc/source
build-dir = doc/build

[tool:pytest]
addopts = --cov anamnesis --cov-report=term-missing
filterwarnings = 
	ignore::DeprecationWarning:h5py.*:

[egg_info]
tag_build = 
tag_date = 0

