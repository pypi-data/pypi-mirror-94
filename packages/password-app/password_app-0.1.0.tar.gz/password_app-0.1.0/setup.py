from setuptools import find_packages, setup

setup(
    name="password_app",
    packages=find_packages(),
    version="0.1.0",
    description="Password frequency prediction simple project based on LSTM model.",
    author="Vitaly",
    license="MIT",
)
